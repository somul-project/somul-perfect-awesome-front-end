createWrapper([
    createTitleText("신청이<br>완료되었습니다.")
]);

createWrapper([
    createHr()
]);

createWrapper([
    createPtag("24시간 안에 이메일 인증을 하고,<br>추가 정보를 입력해주세요.")
]);

createWrapper([
    createButton("yellow", "추가정보 입력하기", "alert('추가정보')")
]);
